# RSM Syndication Feeds — Requirements for StreamX

**Version:** 0.5
**Date:** 2026-08-07
**Owner:** Arbory Digital / RSM IT-CMG
**Audience:** StreamX engineering

---

## 1. Purpose

RSM is migrating rsmus.com from AEM 6.5 to Edge Delivery Services (EDS), authored in
Document Authoring (DA) at `https://da.live/#/rsm-it-cmg/rsm-us`. Three XML syndication
feeds are currently generated by AEM 6.5 Publish and consumed by RSM client portals. They
must continue to be served after the migration, generated instead by **StreamX**, using the
publication path already proven for search (`resource-published` → GitHub Action → StreamX
ingestion).

| Feed | Current URL | Consumed by |
| --- | --- | --- |
| Global (US) | `https://rsmus.com/feeds/us.xml` | myRSM, PartnerSight |
| CorporateSight | `https://rsmus.com/content/feeds/corporatesight.rss` | CorporateSight |
| FamilySight | `https://rsmus.com/feeds/familyoffice.xml` | FamilySight |

All three draw exclusively from the US site. The DA org also hosts `rsm-ca` for the Canadian
site, which has a structurally identical EDS index but is out of scope unless RSM asks for a
Canadian feed (**O-12**).

### 1.1 The core requirement

These are live integrations with downstream portals that RSM does not control. The primary
requirement is not performance, freshness or elegance — it is that

> **the XML shape of each new feed matches the current feed closely enough that no
> downstream consumer has to change anything.**

Element names, nesting, attribute names, value formats, optionality and namespace behaviour
are all part of that contract. §4–§6 specify it element by element. §10 lists places where
the current output is defective and a change may be warranted; each needs consumer sign-off,
because "wrong but expected" beats "correct but breaking".

Everything else in this document serves that requirement.

### 1.2 How to read this document

- **MUST** — required for portal compatibility. Changing it requires consumer sign-off.
- **SHOULD** — strongly preferred; deviate with a documented reason.
- **MAY** — StreamX's discretion. We have deliberately left the *how* open.

We specify what the feeds must contain, not how StreamX builds them. Source of data,
caching, generation trigger, storage and rebuild strategy are StreamX's to design.

### 1.3 Evidence base

Everything in this document was measured directly on **2026-08-07**.

| Source | Used for |
| --- | --- |
| The three live AEM 6.5 feeds | Field inventories, counts, formats, ordering, selection rules (§4–§6) |
| `…aem.page/en/query-index.json` | The EDS index — 4,459 rows × 18 columns (§7) |
| `admin.hlx.page/…/rsm-us/content/query.yaml` | The live index configuration (§7.3, §11) |
| `…adobeaemcloud.com/services/tagsservlet` | Tag taxonomy and label→ID crosswalk — 269 nodes (§9) |
| DA source (`admin.da.live`) | Authoring model, `bios.json`, `redirects.json`, site config (§8.1) |
| Published EDS pages | The `<meta>` model behind the index |
| Live rsmus.com AEM 6.5 pages | The legacy metadata model, for comparison |

**4,095 documents appear in both the Global feed and the EDS index.** Every accuracy figure
in §8 and §9 is measured across that paired set.

---

## 2. Summary of findings

Three findings shape the work. Each needs a decision from RSM.

### 2.1 The tag crosswalk exists and covers two-thirds of the categories

The feeds emit AEM tag IDs (`rsm-sites/content-type/article`); EDS carries display labels
(`RSM Display | Content type / Article`). RSM runs a `tagsservlet` on AEMaaCS holding the
taxonomy and mapping one to the other:

```
https://publish-p194552-e2014559.adobeaemcloud.com/services/tagsservlet
```

It is a **public, unauthenticated JSON endpoint** — directly consumable by StreamX, with no
credentials, export step, or table for anyone to hand-maintain. Each node carries a
`jcr:title` (the display label) and a `path` (`/content/cq:tags/rsm-sites/content-type/article`),
which is the tag ID the feeds emit.

| Approach | Feed categories reproduced |
| --- | --- |
| Servlet crosswalk alone | 15,812 / 22,444 (**70%**) |
| Servlet + slug fallback for uncovered namespaces | 20,257 / 22,444 (**90%**) |
| **Servlet within its actual scope (`rsm-sites`)** | **15,812 / 15,821 (99.94%)** |

Where the servlet is authoritative it is essentially perfect — 9 failures in 15,821. The gap
is one of scope: it exposes only the `rsm-sites` namespace (6 groups, 269 nodes) and returns
an identical payload regardless of query parameters, so the other namespaces cannot be
requested.

| Uncovered namespace | EDS groups | Feed categories |
| --- | --- | --- |
| `rsm-filters` | Organization · Role · Ownership · Country · World region · Location | 3,975 |
| `rsm-internal` | Purpose · Editor · Audience · Framework programs · ENG · Industry conversations | 2,586 |

**The ask is specific: extend the servlet to expose `rsm-filters` and `rsm-internal`**
(**O-1**). That is a scope change to an endpoint which already exists, already holds the
taxonomy, and is already public. Slug derivation reaches 90% in the meantime, and the
residue is mostly tags dropped during migration (§9.3) rather than mapping failures.

### 2.2 The corpus is being deliberately reduced

Of the 8,741 documents in the Global feed:

| Outcome | Count | Detail |
| --- | --- | --- |
| **Migrated to EDS** | **4,095** (47%) | Present in the index at the same path |
| **Deliberately retired** | **3,716** (43%) | Redirected to 194 destinations; 94% collapse to an ancestor section hub |
| **No destination** | **930** (11%) | 721 `/people` · 153 `/insights` · 36 `/offers` · 20 other |

The retirement is systematic — 38 hub pages absorb 3,489 documents:

| Redirect destination | Documents absorbed |
| --- | --- |
| `/insights/tax-alerts` | 946 |
| `/insights/services/business-tax` | 355 |
| `/events` | 300 |
| `/insights/economics` | 257 |
| `/insights/services/risk-fraud-cybersecurity` | 167 |

Only 136 redirects are 1:1 replacements.

Consequences:

- The feeds will legitimately shrink to roughly half their current entry count. Completeness
  must therefore be tested against the migrated corpus (§13.2), or every test reports a
  false 50% loss.
- **The portals must be told.** A consumer doing a full re-sync will see half its catalogue
  disappear. That is a communications task, not an engineering one.
- The 930 with no destination are a genuine gap, mostly `/people` profiles. **O-2**.

### 2.3 Two index defects, each a one-line fix

Both are RSM-side changes in `query.yaml`:

1. **`tags` arrives with no delimiter** — 3,547 of 4,414 tagged rows. The config uses
   `value:` (singular) against a selector that matches multiple elements, which concatenates
   them without a separator. `values:` fixes it. §7.3, **O-3**.
2. **`cardImage` keeps the AEMaaCS host** — 3,833 of 3,839 rows. The sibling `image`
   property strips it with `match(attribute(el, "content"), "https:\/\/[^/]+(/.*)")`;
   `cardImage` uses a bare `attribute()`. Apply the same `match()`. §7.3, **O-4**.

---

## 3. Format and output contract

### 3.1 Formats

| Feed | Format | Requirement |
| --- | --- | --- |
| `us.xml` | **Atom 1.0** (RFC 4287) | MUST remain Atom |
| `familyoffice.xml` | **Atom 1.0** (RFC 4287) | MUST remain Atom |
| `corporatesight.rss` | **RSS 2.0** | MUST remain RSS 2.0 |

The formats are not interchangeable — the portals parse them as-is. Do not harmonise the
three onto one format, and do not modernise one to match another. The two Atom feeds differ
from each other in which elements they emit (§6.2), and that asymmetry is part of the
contract.

StreamX **MAY** additionally expose JSON Feed 1.1 at a parallel URL; it must not replace the
XML.

### 3.2 What "match the current feed" means

For each feed the following **MUST** be preserved exactly, unless §10 says otherwise and a
consumer has signed off:

- **Element names and nesting**, including the non-standard ones (`cardtype`, `cardimage`,
  `cardBgColor`, `cardEyeBrow`) and their current namespace behaviour (§4.2, D-8).
- **Attribute names and spelling**, including lowercase run-together forms such as
  `desktopimage`, `popbackgroundpathdesktop`, `usemessagecontainer` (§4.3).
- **Value formats** — date/time serialisation, absolute vs root-relative URLs, `type="html"`
  on Atom `title`/`summary`, `isPermaLink="false"` on RSS `guid`.
- **Optionality and emptiness behaviour.** Where the current feed emits an empty element
  rather than omitting it, that is observable and must be a deliberate decision.
- **Cardinality**, including where the current feeds exceed their own spec (up to 15
  `<author>` elements per RSS item).

A field-level diff against the current feeds is the acceptance test (§13).

### 3.3 Transport and encoding

- **MUST** serve `Content-Type: text/xml; charset=utf-8` (matches today).
- **MUST** be UTF-8.
- **MUST** be served over HTTPS at the existing paths, including the inconsistent one:
  - `/feeds/us.xml`
  - `/content/feeds/corporatesight.rss` ← note the `/content/` prefix
  - `/feeds/familyoffice.xml`
- **MUST** XML-escape all text content.

### 3.4 Completeness and ordering

Entry counts, for reference when testing completeness (§13.2) — not as a size constraint:

| Feed | Entries today | Expected post-migration (§2.2) |
| --- | --- | --- |
| Global (`us.xml`) | 8,741 | ~4,100 + new content |
| CorporateSight | 1,601 | ~800 + new content |
| FamilySight | 83 | ~29 + new content |

- Feeds are unpaginated and uncapped today, and **MUST** remain so — a portal doing a full
  sync silently loses content if a cap appears. Pagination (RFC 5005) **MAY** be added
  additively; the unpaginated document must remain complete.
- **SHOULD** regenerate on publish, consistent with the existing search pipeline. A few
  minutes of lag is acceptable; the portals poll rather than stream.

| Feed | Current order | Requirement |
| --- | --- | --- |
| CorporateSight | `pubDate` descending (verified strictly sorted); items without `pubDate` last | **MUST** preserve |
| Global | Not chronologically sorted | SHOULD become date-descending, with sign-off |
| FamilySight | Not chronologically sorted | SHOULD become date-descending, with sign-off |

---

## 4. Feed 1 — Global (`/feeds/us.xml`)

Atom 1.0. Consumed by myRSM and PartnerSight. 8,741 entries.

### 4.1 Feed-level elements (current output)

```xml
<feed xmlns="http://www.w3.org/2005/Atom">
  <id></id>                                        <!-- EMPTY - defect, see §10 -->
  <updated>2026-08-07 12:21:26.353 UTC</updated>   <!-- not RFC 3339 - defect -->
  <title type="html">Global Feed Service</title>
  <author>
    <email>info@rsmus.com</email>
    <name>RSM US LLP</name>
  </author>
  <link href="" rel="self"/>                       <!-- EMPTY - defect -->
</feed>
```

| Element | Required | Current value |
| --- | --- | --- |
| `feed/title` | MUST | `Global Feed Service` |
| `feed/author/name` | MUST | `RSM US LLP` |
| `feed/author/email` | MUST | `info@rsmus.com` |
| `feed/updated` | MUST | Generation timestamp |
| `feed/id` | MUST | Currently empty — SHOULD be populated (§10) |
| `feed/link[@rel=self]` | SHOULD | Currently empty — SHOULD be populated (§10) |

### 4.2 Entry-level elements

Counts out of 8,741 entries.

| Element / attribute | Present | Cardinality | Notes |
| --- | --- | --- | --- |
| `title` (`type="html"`) | 8,741 | 1 | |
| `link/@href` (no `rel`) | 8,741 | 1 | Absolute `https://rsmus.com/…​.html` |
| `id` | 8,740 | 1 | 64-char lowercase hex (SHA-256), opaque. 1 empty |
| `published` | 8,741 | 1 | ISO-8601 with offset, e.g. `2024-03-18T00:00:00.000+05:30`. **2,445 empty** |
| `updated` | 8,741 | 1 | `2026-07-09 05:02:24.464 UTC` — **not RFC 3339** |
| `summary` (`type="html"`) | 7,903 | 0–1 | 837 absent |
| `category/@term` | 47,288 total | 0–n | Tag ID, `/`-delimited. Avg ~5.4/entry |
| `contributor/name` | 6,392 total, on 3,508 entries | 0–15 | Author display name |
| `contributor/link` | 4,497 | 0–1 per contributor | Bio URL. **1,895 contributors have none** |
| `cardtype` | 7,500 | 0–1 | `featuredCard` 7,360 · `solutionCard` 106 · `insightsCard` 32 · `featureCard` 2 |
| `cardimage` | 7,386 | 0–1 | **Root-relative** DAM path, e.g. `/content/dam/rsm/…_crd.jpg` |
| `cardBgColor` | 7,500 | 0–1 | `noColor` 3,601 · `rsmBlue` 2,061 · `rsmGreen` 1,044 · `rsmGray` 737 · `rsmLightGray` 24 · `rsmLightGreen` 17 · `rsmLightBlue` 16 |
| `cardEyeBrow` | 851 | 0–1 | e.g. `Article`, `Tax alert`, `Video`. Inconsistently cased in source |
| `link[@rel="enclosure"]` | 16,192 total | 0–n | See §4.3 |

`cardtype`, `cardimage`, `cardBgColor` and `cardEyeBrow` are emitted **in the Atom
namespace**, because the feed declares Atom as default and these are unprefixed. This is
invalid Atom and part of the current contract — see §10, D-8.

### 4.3 `link[@rel="enclosure"]` variants

| `@type` | Count | Attributes emitted |
| --- | --- | --- |
| `featuredCard` | 7,360 | `href`, `bgcolor` |
| `sm-banner` | 6,382 | `title`, `subtext`, `href`, `desktopimage`, `mobileimage`, `popbackgroundpathdesktop`, `hotspottext`, `hotspoturl`, `gradientoverlay`, `popbackgroundcolor`, `usemessagecontainer`, `msgcontainercolor`, `primaryctalabel`, `primaryctaurl`, `secondaryctalabel`, `secondaryctaurl` |
| *(empty string)* | 1,252 | varies |
| `md-banner` | 1,063 | as `sm-banner` |
| `solutionCard` | 106 | `href`, `bgcolor` |
| `insightsCard` | 32 | `href`, `bgcolor` |
| `hero` | 3 | as `sm-banner` |
| `featureCard` | 2 | `href`, `bgcolor` |

The `sm-banner` / `md-banner` enclosures carry 16 attributes describing an AEM 6.5 hero
component. The DA authoring model has no corresponding fields (§8.1), and nothing in the
index or page metadata maps to them. If the portals need this data it must be authored from
scratch. **O-5**.

---

## 5. Feed 2 — CorporateSight (`/content/feeds/corporatesight.rss`)

RSS 2.0. 1,601 items.

### 5.1 Channel-level elements (current output)

```xml
<rss version="2.0">
  <channel>
    <title>CorporateSight Feed</title>
    <description>CorporateSight Insight Feed</description>
    <lastBuildDate>Fri, 07 Aug 2026 11:43:37 UTC</lastBuildDate>
    <link>https://rsmus.com/feeds/corporatesight.rss</link>   <!-- WRONG - see §10 -->
  </channel>
</rss>
```

| Element | Required | Current value |
| --- | --- | --- |
| `channel/title` | MUST | `CorporateSight Feed` |
| `channel/description` | MUST | `CorporateSight Insight Feed` |
| `channel/link` | MUST | Does not match the URL the feed is served from (`/content/feeds/…`) |
| `channel/lastBuildDate` | SHOULD | Generation timestamp |

### 5.2 Item-level elements

Counts out of 1,601 items.

| Element / attribute | Present | Cardinality | Notes |
| --- | --- | --- | --- |
| `title` | 1,601 | 1 | Plain text (no `type` attribute) |
| `link` | 1,601 | 1 | Absolute `https://rsmus.com/…​.html` |
| `guid` (`isPermaLink="false"`) | 1,601 | 1 | **UUID** — AEM `jcr:uuid` |
| `description` | 1,571 | 0–1 | 28 absent |
| `pubDate` | 1,553 | 0–1 | RFC-822-style, e.g. `Thu, 20 Aug 2026 04:00:00 UTC`. **46 absent** |
| `author` | 2,606 total, on 1,198 items | **0–15** | **Plain display name**, not an email |
| `category` | 14,938 total | 0–n | Tag ID. Avg ~9.3/item |
| `enclosure/@url` | 1,596 | 0–1 | **Absolute** URL, card rendition. **No `type` or `length`** |

Asymmetries versus the Atom feeds — real, and **MUST** be preserved:

- `enclosure/@url` is **absolute**; Atom `cardimage` is **root-relative**.
- Authors are flat `<author>` strings; Atom uses structured `<contributor><name>/<link>`.
- No card styling fields at all.
- `pubDate` extends into the future (latest 2026-08-20) — events are syndicated ahead of
  their date. **MUST NOT** filter out future-dated items.

---

## 6. Feed 3 — FamilySight (`/feeds/familyoffice.xml`)

Atom 1.0. 83 entries. Structurally a subset of the Global feed's schema.

### 6.1 Feed-level elements (current output)

```xml
<feed xmlns="http://www.w3.org/2005/Atom">
  <id></id>
  <updated>2026-08-07 12:21:50.553 UTC</updated>
  <title type="html">Family Office</title>
  <author>
    <email>test@gmail.com</email>     <!-- PLACEHOLDER DATA IN PRODUCTION -->
    <name>test</name>                 <!-- PLACEHOLDER DATA IN PRODUCTION -->
  </author>
  <link href="" rel="self"/>
</feed>
```

**Defect D-1:** the FamilySight channel author is literally `test` / `test@gmail.com` in
production. This **MUST** be corrected. Proposed: name `RSM US LLP`, email `info@rsmus.com`.
RSM to confirm the channel title (`Family Office` today).

### 6.2 Entry-level elements

Counts out of 83 entries.

| Element / attribute | Present | Cardinality | Notes |
| --- | --- | --- | --- |
| `title` (`type="html"`) | 83 | 1 | |
| `link/@href` | 83 | 1 | Absolute |
| `id` | 83 | 1 | **UUID** — not the SHA-256 scheme the Global feed uses |
| `published` | 83 | 1 | ISO-8601 with offset. **5 empty** |
| `updated` | 83 | 1 | `… UTC` format, not RFC 3339 |
| `summary` (`type="html"`) | 83 | 1 | Always present |
| `category/@term` | 470 total | 1–n | Avg ~5.7/entry |
| `contributor/name` | 74 total, on 39 entries | 0–4 | 8 contributors have no `link` |
| `cardtype` | 83 | 1 | **Always `featuredCard`** |
| `cardimage` | 83 | 1 | Root-relative DAM path |

Not emitted here, unlike Global: `cardBgColor`, `cardEyeBrow`, and all
`link[@rel="enclosure"]` elements. This asymmetry is part of the contract (§3.2).

### 6.3 Identifier schemes differ per feed

| Feed | Scheme | Same document, same ID? |
| --- | --- | --- |
| Global | SHA-256 hex (opaque) | — |
| CorporateSight | UUID (`jcr:uuid`) | ✅ matches FamilySight |
| FamilySight | UUID (`jcr:uuid`) | ✅ matches CorporateSight |

Of 1,544 documents in both Global and CorporateSight, **zero** share an ID. Of 8 documents
in both CorporateSight and FamilySight, **7** share an identical UUID.

**Identifiers MUST be stable for the lifetime of a document.** Portals deduplicate on them;
a changed GUID re-surfaces content as new.

`jcr:uuid` does not exist in EDS or DA — neither the index nor the DA authoring model
carries a UUID field. A deterministic replacement is required, e.g. UUIDv5 over the document
path with a fixed namespace. **O-6**.

### 6.4 Feed membership / selection rules

| Feed | Rule | Status |
| --- | --- | --- |
| **FamilySight** | Tag `rsm-sites/service/family-office-services` | ✅ Confirmed, and it survives migration |
| **Global** | "All published, syndicatable pages" | Medium confidence |
| **CorporateSight** | No derivable rule | ❌ Unresolved — blocking |

**FamilySight** is verified end to end. All 83 entries carry
`rsm-sites/service/family-office-services`, the only tag common to all. In EDS, exactly **29**
documents carry `RSM Display | Service / Family office services`, and those are exactly the
29 FamilySight documents that migrated — a clean 1:1. The tag sits in the `rsm-sites`
namespace, so the servlet crosswalk resolves it authoritatively (§9). This makes FamilySight
the right pilot (§13.3).

**Global** spans `/insights` 5,930 · `/people` 1,229 · `/events` 481 · `/services` 328 ·
`/newsroom` 262 · `/technologies` 208 · `/about` 66 · `/careers` 56 · `/industries` 42 ·
`/offers` 37 · `/locations` 33 · `/middle-market` 27. No common tag.

**CorporateSight** has no common tag. It is a strongly tax-oriented subset — comparing its
1,509 `/insights` items against the 4,466 `/insights` items in the Global feed that are not
in it:

| Tag | In CorporateSight | Not in CorporateSight |
| --- | --- | --- |
| `rsm-sites/service/business-tax/federal-tax` | 69% | 0% |
| `rsm-sites/content-type/alert` | 49% | 13% |
| `rsm-internal/purpose/compliance` | 43% | 15% |
| `rsm-filters/role/tax-executive` | 40% | 11% |
| `rsm-sites/service/business-tax` | 39% | 10% |
| `rsm-filters/role/cfo` | 33% | 12% |

That is a signature, not a rule — no tag is on 100% of items. Most likely an AEM query with
an OR-set of tags, or a curated list. **Do not reverse-engineer it from these statistics.**

Two factors compound the problem. CorporateSight's largest single redirect destination is
`/insights/tax-alerts`, which absorbed 946 retired documents (§2.2) — and tax alerts are 49%
of the feed, so a large fraction of its catalogue has been retired. Separately, three of the
six tags that most distinguish it sit in `rsm-filters` / `rsm-internal`, which the servlet
does not yet cover (§2.1). **O-7**.

Additional verified facts:

- 55 of 1,601 CorporateSight items and 5 of 83 FamilySight entries are not in the Global feed
  — Global is **not** a strict superset.
- CorporateSight spans 2011–2026. There is no date-window filter; do not add one.

---

## 7. What EDS and DA provide

### 7.1 The EDS index

`https://main--rsm-us--rsm-it-cmg.aem.page/en/query-index.json` — 4,459 rows, 18 columns.
Built by the `rsmus-en` index in `query.yaml`, which includes `/**` and excludes
`/drafts/**`, `/docs/**`, `/private/**`, `/fragments/**`.

The index is served under `/en/` but its `path` values are **not** `/en/`-prefixed — they are
site-root paths (`/insights/…`), and pages resolve there. `/en/…` returns 404.

| Column | Non-empty | DA metadata field | Notes |
| --- | --- | --- | --- |
| `path` | 4,459 (100%) | — | Site-root, extensionless |
| `title` | 4,459 (100%) | `Title` | via `og:title` |
| `tags` | 4,414 (99%) | `Tags` | ⚠️ no delimiter — §7.3 |
| `description` | 4,313 (97%) | `Description` | |
| `cardLabel` | 3,941 (88%) | `Card Label` | `Read more` 2,856 · `Learn more` 865 · `Watch` 167 · `Register` 51 |
| `cardImage` | 3,839 (86%) | `Card Image` | ⚠️ AEMaaCS host — §7.3 |
| `image` | 3,644 (82%) | `Image` | Root-relative (host stripped in config). Social rendition, not card |
| `card-resource` | 3,507 (79%) | `Card Resource` | Content-type tag — the eyebrow source |
| `date` | 2,994 (67%) | `Date` | ISO `YYYY-MM-DD`. 1 malformed (`2024-01`) |
| `page-title` | 2,965 (66%) | `Page Title` | |
| `card-tags` | 2,965 (66%) | `Card Tags` | Comma-delimited in most rows |
| `cardTitle` | 2,520 (57%) | `Card Title` | |
| `contributor` | 1,815 (41%) | `Contributor` | Comma-separated **slugs** |
| `navigation-title` | 1,101 (25%) | `Navigation Title` | |
| `color` | 1,030 (23%) | `Color` | `blue` 552 · `green` 323 · `gray` 151 · 4 legacy `rsmX` values |
| `card-icon` | 563 (13%) | `Card Icon` | |
| `redirectTarget` | **0 (0%)** | — | Empty in every row |
| `offDateTime` | **0 (0%)** | `On-Off Time` | Field exists in authoring but is unused |

### 7.2 What is not available anywhere

| Feed field | Status |
| --- | --- |
| `updated` (modification time) | No column, no DA field. Derive from HTTP `Last-Modified` at index time |
| `id` / `guid` | No UUID anywhere. Must be synthesised (§6.3) |
| `cardtype` (`featuredCard` etc.) | No equivalent. A constant `featuredCard` covers 98% of current values |
| `sm-banner` / `md-banner` + 16 attributes | No equivalent in the index, the page, or the DA authoring model |

### 7.3 Index defects, with root cause

**Defect E-1 — `tags` has no delimiter.** 3,547 of 4,414 tagged rows concatenate tags with no
separator:

```
RSM Display | Service / Business taxRSM Display | Service / Managed servicesRSM Display | …
```

The live config explains why:

```yaml
      # CHANGED: single pipe-delimited tag string from the DA picker plugin.
      # One meta -> one string. Split on "|" in your cardlist block JS.
      tags:
        select: head > meta[property="article:tag"]
        value: |
          attribute(el, "content")
```

The comment records an assumption — one `<meta>` holding one pipe-delimited string — that
the rendered pages do not satisfy. They emit N separate `<meta property="article:tag">`
elements, one per tag, and `value:` (singular) against a multi-match selector concatenates
them without a separator. The assumption is also self-defeating: the labels themselves
contain a pipe (`RSM Display | Service / Business tax`), so splitting on `|` would split
inside every tag.

**Fix:** `values:` instead of `value:`. Source data and page markup are already correct — DA
stores each tag as its own `<p>`, and the page renders each as its own `<meta>`.

Splitting on the literal `RSM Display | ` currently works but is fragile: 2 rows already use
variant prefixes (`RSM Display :`, `RSM|Display`) that break it. StreamX **MUST NOT** be
asked to rely on that. **O-3**.

**Defect E-2 — `cardImage` keeps the AEMaaCS host.** 3,833 of 3,839 values are absolute URLs
on `publish-p194552-e2014559.adobeaemcloud.com`. The config shows the inconsistency directly
— `image` strips the host, `cardImage` does not:

```yaml
      image:
        select: head > meta[property="og:image"]
        value: |
          match(attribute(el, "content"), "https:\/\/[^/]+(/.*)")     # host stripped
      cardImage:
        select: head > meta[name="card-image"]
        value: |
          attribute(el, "content")                                    # host kept
```

The DAM genuinely lives in AEMaaCS — the DA site config records `aem.repositoryId` =
`author-p194552-e2014559.adobeaemcloud.com` — so the asset host is real, but the feeds need
public `rsmus.com` URLs. Applying the same `match()` to `cardImage` makes it consistent with
`image` and with what the Atom feeds expect (root-relative). **O-4**.

**Defect E-3 — taxonomy hygiene.** 428 distinct labels are in use against 269 servlet nodes.
One row uses `RSM Display :` instead of `|`, one uses `RSM|Display`, and one value is a
mangled concatenation of three tags. Group names are inconsistent (`Service` vs `Services`,
a stray `Type`). One `date` is `2024-01` rather than ISO.

### 7.4 Author names — `/data/bios.json`

DA holds a 1,413-row bios sheet at `/data/bios.json`, keyed on `Unique ID`, which is exactly
the contributor slug used in page metadata. Columns include `First Name`, `Last Name`,
`Job Title` and `Bio Page Path`.

| Metric | Result |
| --- | --- |
| Slugs resolved to a name | **661 / 733 (90%)** |
| Resolved by occurrence | **3,827 / 3,932 (97%)** |
| `First Name` + `Last Name` matches the current feed's contributor name | **2,923 / 3,324 (88%)** |
| Slugs with a `Bio Page Path` (for `contributor/link`) | **270 / 733 (37%)** |
| Those bio paths that exist in the EDS index | **264 / 270** |

The 12% name mismatches are almost entirely double-space artifacts in the AEM feed
(`Casey  Chapman` vs `Casey Chapman`) — the bios value is the cleaner one.

`bios.json` is the source for `contributor/name` and `author`. Bio links remain partially
unresolved at 37%. **O-8**.

---

## 8. Field mapping

Measured across the 4,095 documents present in both the Global feed and the EDS index.
Accuracy is the proportion where the EDS value exactly reproduces the current feed value.

### 8.1 The DA authoring model

Every syndicatable page carries a `metadata` block with these 21 fields:

```
Page Title · Navigation Title · Title · Subtitle · Date · Contributor · Description
Image · Card Title · Card Image · Card Icon · Card Label · Tags · Card Tags
Card Resource · Social Share · Robots · Canonical URL · Authoring Comment
On-Off Time · Color
```

Note what is absent: no card-type field, no hero/banner fields, no UUID, no modification
timestamp. `Robots` and `Canonical URL` may bear on feed inclusion (**O-9**); `On-Off Time`
is an embargo/expiry field, currently unused, which feeds **SHOULD** respect if populated.

### 8.2 Atom feeds (Global, FamilySight)

| Feed element | Source | Coverage | Accuracy | Verdict |
| --- | --- | --- | --- | --- |
| `entry/title` | index `title` | 100% | **90%** | ✅ Use `title`. Not `page-title` (24%) or `cardTitle` (17%) |
| `entry/link/@href` | index `path` | 100% | — | ✅ Prefix with canonical host; §8.4 |
| `entry/summary` | index `description` | 97% | **77%** | ✅ Best available. 23% differ — post-migration edits |
| `entry/category/@term` | index `tags` + **tagsservlet** | 99% | **70% / 99.94% on `rsm-sites`** | ⚠️ §9 |
| `entry/published` | index `date` | 67% | **98%** | ✅ Reliable where present |
| `entry/updated` | — | — | — | ❌ Derive from HTTP `Last-Modified` |
| `entry/id` | — | — | — | ❌ Must be synthesised (§6.3) |
| `entry/contributor/name` | **`bios.json`** via index `contributor` | **97%** by occurrence | **88%** | ✅ §7.4 |
| `entry/contributor/link` | `bios.json` → `Bio Page Path` | **37%** | — | ⚠️ **O-8** |
| `entry/cardimage` | index `cardImage` | 86% | **100%** | ✅ DAM path identical in 3,524/3,530. Strip host (E-2) |
| `entry/cardBgColor` | index `color` | 23% | **100%** | ✅ `blue`→`rsmBlue`, `green`→`rsmGreen`, `gray`→`rsmGray`, absent→`noColor` |
| `entry/cardEyeBrow` | index `card-resource` | 79% | **79%** | ⚠️ Only 6% of feed entries have one today |
| `entry/cardtype` | — | — | — | ❌ No source. A constant `featuredCard` covers 98% |
| `sm-banner` / `md-banner` | — | — | — | ❌ No source anywhere. **O-5** |

On `card-resource` as the eyebrow source, the config carries a relevant note:

```yaml
      # Content-type eyebrow printed on the card ("ARTICLE" / "CASE STUDY").
      # Legacy reads cardResource directly; deriving it from cq:tags diverges on
      # 325 pages and is the ONLY content-type signal on 163 of them.
```

`card-resource` is therefore authoritative and **MUST NOT** be re-derived from the tag list.

On `published` / `date`: of 2,746 paired documents where both are present, 2,691 (98%) agree
exactly. All 55 disagreements are off-by-one-day, caused by the AEM feed emitting spurious
timezone offsets (`+05:30`, `+07:00`) against a midnight timestamp. The EDS `date` is the
more trustworthy value.

Coverage is perfectly correlated: **zero** documents have a feed `published` but no index
`date`, and **zero** the reverse. The ~33% with no date never had one. **O-10**.

### 8.3 RSS feed (CorporateSight)

| Feed element | Source | Notes |
| --- | --- | --- |
| `item/title` | index `title` | Plain text, no `type` attribute |
| `item/link` | index `path` | Absolute URL |
| `item/description` | index `description` | |
| `item/guid` | — | Synthesise; `isPermaLink="false"` (§6.3) |
| `item/pubDate` | index `date` | Convert ISO → RFC 822 |
| `item/author` | `bios.json` via `contributor` | Flat display names (§7.4) |
| `item/category` | index `tags` + tagsservlet | §9 |
| `item/enclosure/@url` | index `cardImage` | **Absolute** URL — note this differs from Atom |

### 8.4 URL mapping

Current feed links end in `.html`. EDS pages are extensionless, and their canonical `og:url`
points at a different host:

```
AEM 6.5 : https://rsmus.com/insights/…/compensation-…-productivity.html
EDS     : https://eds-prd.rsmus.com/insights/…/compensation-…-productivity
```

DA's `redirects.json` holds 4,788 redirects, and **not one handles `.html`** — every `Source`
and `Destination` is already extensionless. That map exists to consolidate retired content
(§2.2), not to preserve legacy URL form.

`.html` preservation is therefore currently unaddressed. Unless RSM adds a rewrite rule,
every stored link in every consuming portal breaks at cutover. **O-11**.

---

## 9. Tags — using the crosswalk

### 9.1 How the mapping works

`tagsservlet` returns a tree of nodes, each with a `jcr:title` and a `path`. Concatenating
the titles down the tree gives exactly the EDS label, minus its constant `RSM Display | `
prefix; the `path` gives the tag ID the feeds emit:

| EDS label (index `tags`) | Servlet node path | Feed `<category>` |
| --- | --- | --- |
| `RSM Display \| Content type / Article` | `/content/cq:tags/rsm-sites/content-type/article` | `rsm-sites/content-type/article` |
| `RSM Display \| Service / Family office services` | `/content/cq:tags/rsm-sites/service/family-office-services` | `rsm-sites/service/family-office-services` |
| `RSM Display \| Site / US` | `/content/cq:tags/rsm-sites/site/us` | `rsm-sites/site/us` |

`RSM Display` is a **constant prefix, not a namespace marker** — labels from all three AEM
namespaces carry it, so the namespace is not recoverable from the label alone. That is
precisely what the servlet supplies.

Implementation is a single lookup table built at startup and refreshed periodically; the
payload is 36 KB.

### 9.2 Coverage

| Scope | Result |
| --- | --- |
| Servlet nodes | 269, `rsm-sites` only (6 groups: Site, Content type, Service, Industry, Technology, Topic) |
| Distinct EDS labels resolved | 255 / 428 |
| EDS tag occurrences resolved | 17,378 / 24,326 (71%) |
| **Feed categories reproduced — `rsm-sites`** | **15,812 / 15,821 (99.94%)** |
| Feed categories reproduced — overall | 15,812 / 22,444 (70%) |
| Overall with slug-derivation fallback | 20,257 / 22,444 (90%) |

The servlet ignores query parameters — `?namespace=`, `?path=` and `?root=` all return the
identical 36 KB `rsm-sites` payload — so the other namespaces cannot be requested today.

### 9.3 What remains, and what to ask for

| Uncovered namespace | EDS groups | Feed categories |
| --- | --- | --- |
| `rsm-filters` | Organization · Role · Ownership · Country · World region · Location | 3,975 |
| `rsm-internal` | Purpose · Editor · Audience · Framework programs · ENG · Industry conversations | 2,586 |

Recommendation, in priority order:

1. **Extend `tagsservlet` to expose `rsm-filters` and `rsm-internal`** (**O-1**). This is the
   whole fix — a scope change to an endpoint that already exists, already holds the taxonomy,
   and is already public.
2. **Until then, fall back to slug derivation** for those two namespaces: lowercase the
   label, hyphenate, and prefix the namespace implied by the group. That reaches 90% overall.
3. **Accept the residue.** After both, ~10% remains unreproduced: 1,110 `rsm-internal` and
   1,006 `rsm-filters` occurrences that are absent from the migrated document entirely —
   tags dropped during migration, not mapping failures — plus 62 legacy junk values
   (`default/corporate_tax_services`, `primary/tag5`, `resource-type/article`) that are
   correctly gone.

No crosswalk fixes a tag that was never applied. If the portals depend on
`rsm-filters/organization/*` for filtering, that is a content problem (§2.2, **O-2**), not a
mapping one.

---

## 10. Defects in the current feeds

The default is **preserve bug-for-bug** unless the consuming portal team signs off — these
feeds have live consumers that may parse defensively around them (§3.2).

| # | Defect | Feeds | Spec violated | Recommendation |
| --- | --- | --- | --- | --- |
| D-1 | Channel author is `test` / `test@gmail.com` | FamilySight | — | **Fix.** Placeholder data in production |
| D-2 | Channel `link` is `/feeds/corporatesight.rss`; served from `/content/feeds/…` | CorporateSight | RSS 2.0 | **Fix** — cheap, low risk |
| D-3 | Timestamps formatted `2026-08-07 12:21:26.353 UTC` | Both Atom | RFC 4287 requires RFC 3339 | **Fix** with sign-off — strict parsers reject this today |
| D-4 | `pubDate` uses `UTC` as timezone token | CorporateSight | RFC 822 wants `GMT` / `+0000` | **Fix** with sign-off |
| D-5 | Feed-level `<id>` empty | Both Atom | RFC 4287 — REQUIRED | **Fix.** Use the feed's own URL |
| D-6 | `<link href="" rel="self"/>` empty | Both Atom | RFC 4287 SHOULD | **Fix.** Use the feed's own URL |
| D-7 | `<published></published>` empty — 2,445 Global, 5 FamilySight | Both Atom | RFC 4287 | **Fix at source** (O-10), or omit rather than emit empty |
| D-8 | Card elements emitted in the Atom namespace | Both Atom | RFC 4287 extension rules | **Fix** → `xmlns:rsm`. **Highest-risk change** — portals may match unprefixed names |
| D-9 | Multiple `<author>` per item | CorporateSight | RSS 2.0 allows one | **Preserve**, or move to `<dc:creator>` with sign-off |
| D-10 | `<author>` holds a name, not an email | CorporateSight | RSS 2.0 | **Preserve.** Portals clearly expect names |
| D-11 | `<enclosure>` has no `type` / `length` | CorporateSight | RSS 2.0 requires both | **Add** `type`; `length` may be `0` |
| D-12 | `<enclosure url= "…">` — stray space | CorporateSight | cosmetic | **Fix** |
| D-13 | 1 Global entry has empty `<id>` | Global | RFC 4287 | **Fix** |

StreamX **SHOULD** validate output against the W3C Feed Validation Service and report what it
fixed versus preserved.

---

## 11. Recommended architecture

Build the feeds from a published EDS index. StreamX needs no DA write access, and DA read
access only for `bios.json`.

The two external inputs are already available without credentials or export steps:

| Input | Access |
| --- | --- |
| `tagsservlet` — label → tag ID (§9) | Public, unauthenticated, 36 KB |
| `bios.json` — slug → author name (§7.4) | DA read, 747 KB, changes rarely |

Both should be cached and refreshed periodically rather than fetched per document.

The current `rsmus-en` index is not sufficient on its own: it lacks `updated`, a stable ID
and `cardtype`; its `tags` column is unusable (E-1); and its `cardImage` keeps the AEMaaCS
host (E-2).

**Recommendation: add dedicated feed indices to `query.yaml`** rather than changing
`rsmus-en`, which the site itself depends on. This keeps feed membership (§6.4) an
`include`/`exclude` concern and avoids destabilising the live site index. The two defect
fixes should still be made in `rsmus-en` for the site's own benefit.

Deltas from the current `rsmus-en` config:

```yaml
indices:
  feed-global:
    include:
      - /insights/**
      - /events/**
      - /services/**
      - /newsroom/**
      - /technologies/**
      - /people/**
      # …see §6.4 for the full section list
    exclude:
      - /drafts/**
      - /docs/**
      - /private/**
      - /fragments/**
    target: /feeds/global-index.json
    properties:
      # FIX E-1: `values:` (plural). Pages already emit one <meta> per tag;
      # `value:` concatenates them with no separator.
      tags:
        select: head > meta[property="article:tag"]
        values: |
          attribute(el, "content")

      # FIX E-2: the same host-stripping `image` already uses.
      cardImage:
        select: head > meta[name="card-image"]
        value: |
          match(attribute(el, "content"), "https:\/\/[^/]+(/.*)")

      # NEW: required for Atom <updated>; no DA field carries it.
      lastModified:
        select: none
        value: |
          parseTimestamp(headers['last-modified'], 'ddd, DD MMM YYYY hh:mm:ss GMT')

      # …remaining properties as per the current rsmus-en index
```

StreamX **MAY** propose a different source — for example consuming the published-page events
it already receives and maintaining its own materialised feed store — if that fits its
architecture better. We care about the output contract (§3.2), not the pipeline.

**Constraints:** a feed **MUST NOT** contain an unpublished document, and an unpublish
**MUST** remove it from all feeds, mirroring `unpublish-from-streamx.yaml`. Feeds **SHOULD**
honour `On-Off Time` and `Robots` if those become populated (**O-9**).

---

## 12. Open questions for RSM

| # | Question | Blocks |
| --- | --- | --- |
| **O-1** | Extend `tagsservlet` to expose `rsm-filters` and `rsm-internal`? Covers the remaining 30% of feed categories (§9.3) | Category fidelity, all three feeds |
| **O-2** | Is the 3,716-document retirement (§2.2) final, and have the portals been told they will lose roughly half their catalogue? What about the 930 with no destination (721 `/people`)? | All three feeds |
| **O-3** | Fix E-1 — `values:` instead of `value:` for `tags` in `query.yaml` | All three feeds |
| **O-4** | Fix E-2 — apply `image`'s host-stripping `match()` to `cardImage`. And what is the public asset host at cutover? | Image fields |
| **O-5** | Do myRSM/PartnerSight consume `sm-banner`/`md-banner`? No source data exists in EDS or DA — if needed, it must be authored from scratch | Global feed |
| **O-6** | GUID strategy: preserve AEM `jcr:uuid` via a migration map, or mint deterministic IDs and accept a one-off portal re-sync? | All three feeds |
| **O-7** | Authoritative CorporateSight selection rule — please export the AEM 6.5 feed/query configuration | CorporateSight |
| **O-8** | Bio links resolve for only 37% of contributors. Backfill `Bio Page Path` in `bios.json`, or omit `contributor/link` where unknown? | `contributor/link` |
| **O-9** | Should feeds honour `Robots`, `Canonical URL` and `On-Off Time` from the DA metadata block? | Feed inclusion logic |
| **O-10** | Will the ~33% of documents with no publication date be backfilled? | Ordering, `published`/`pubDate` |
| **O-11** | Will public URLs keep `.html`? `redirects.json` does not handle it. And will the host be `rsmus.com` or `eds-prd.rsmus.com`? | All three feeds |
| **O-12** | Is a Canadian feed wanted from `rsm-ca`? All current feeds are US-only | Scope |
| **O-13** | Who signs off on the §10 defect fixes for each portal? | Cutover |

---

## 13. Acceptance criteria

### 13.1 A feed is accepted when

1. **Shape-faithful.** For a 50-document stratified sample, every element, attribute and
   value format in §4–§6 matches the current feed, except where §10 or an open question says
   otherwise. This is the primary test (§3.2).
2. **Well-formed and valid.** Parses as XML; validates as Atom 1.0 / RSS 2.0 modulo the §10
   items explicitly agreed as "preserve".
3. **Complete against the migrated corpus** — §13.2.
4. **Correctly scoped.** Membership matches §6.4 / O-7.
5. **Stable identifiers.** Two consecutive generations with no content change produce
   identical GUIDs. Editing a document does not change its GUID.
6. **Correctly ordered.** CorporateSight strictly `pubDate` descending.
7. **Responsive.** A publish appears within the agreed window; an unpublish removes the
   document from every feed.
8. **Portal-verified.** Each consuming portal confirms successful ingestion from a staging
   URL before cutover. This is the test that matters most.

### 13.2 Completeness is measured against the migrated corpus

Because of §2.2, a naive "does the new feed contain everything the old feed did?" test
reports roughly 50% loss and is not useful. Test instead that every document in the current
feed falls into exactly one bucket, and that the buckets are the expected size:

| Bucket | Expected | Test |
| --- | --- | --- |
| Migrated | ~4,095 | Present in the new feed |
| Deliberately retired | ~3,716 | Absent from the feed; present in `redirects.json` with a live destination |
| No destination | ~930 | Absent from both — **flagged for RSM review, not silently dropped** |

Anything falling into no bucket is a real defect.

### 13.3 Suggested delivery sequence

| Phase | Content |
| --- | --- |
| 0 | RSM resolves O-1 … O-4: servlet extended, E-1/E-2 fixed, asset host confirmed |
| 1 | RSM resolves O-5 … O-11. Feed indices added to `query.yaml` (§11) |
| 2 | **FamilySight first** — 83 entries, simplest schema, an unambiguous selection rule verified to survive migration, and a selector tag inside the servlet's covered namespace. Proves the pipeline end to end |
| 3 | **CorporateSight** — once O-7 is answered and the §2.2 impact on its catalogue is understood |
| 4 | **Global** — largest and most complex; scope depends on O-5 |
| 5 | Parallel run: new and old feeds served simultaneously, field-level diff per §13.1 and bucket-diff per §13.2, portal verification |
| 6 | Cutover |

---

## Appendix A — Reference material

| Resource | URL | Auth |
| --- | --- | --- |
| Tag taxonomy / crosswalk | `https://publish-p194552-e2014559.adobeaemcloud.com/services/tagsservlet` | **None — public** |
| EDS index (US) | `https://main--rsm-us--rsm-it-cmg.aem.page/en/query-index.json` | Site token |
| Index config (US) | `https://admin.hlx.page/config/rsm-it-cmg/sites/rsm-us/content/query.yaml` | `x-auth-token` |
| Index config (Canada) | `https://admin.hlx.page/config/rsm-it-cmg/sites/rsm-ca/content/query.yaml` | `x-auth-token` |
| DA source | `https://admin.da.live/source/rsm-it-cmg/rsm-us/…` | DA token |
| — author bios | `…/rsm-us/data/bios.json` — 1,413 records | DA token |
| — redirects | `…/rsm-us/redirects.json` — 4,788 entries | DA token |

DA org sites: **`rsm-us`** (US — the source for all three current feeds), **`rsm-ca`**
(Canada), plus `rsm-da`, `rsm-da-tools`, `rsm-review`, `rsm-seahawks`.

Also referenced:

- StreamX EDS publish pipeline POC: `arbory-digital-inc/arbory-dev` —
  `.github/workflows/streamx-publish.yaml`, `unpublish-from-streamx.yaml`, and the
  "Search: tags, categories and facets" section of `README.md`.
- AEM 6.5 feeds as inspected 2026-08-07.
